services:
  dtdl-visualiser:
    build:
      context: .
    volumes:
      - ${LOCAL_ONTOLOGY_PATH}:/mnt/ontology
    ports:
      - '3000:3000'
    cap_add:
      - SYS_ADMIN
    command: ['dtdl-visualiser', 'parse', '-p', '/mnt/ontology']
